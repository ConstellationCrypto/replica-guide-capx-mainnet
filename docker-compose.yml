version: "3.4"

volumes:
  l2_data:

services:
  l2:
    image: offchainlabs/nitro-node:v3.6.7-a7c9f1e
    platform: linux/amd64
    ports:
      - "8545:8545"
      - "8546:8546"
    volumes:
      - "${PWD}/nodeConfig.json:/config/nodeConfig.json"
      - l2_data:/home/user/.arbitrum
    entrypoint:
      - "/usr/local/bin/nitro"
      - --conf.file=/config/nodeConfig.json
      - --execution.forwarding-target=wss://capx-mainnet.calderachain.xyz/ws
      - --node.feed.input.url=wss://capx-mainnet.calderachain.xyz/feed
      - --node.data-availability.rest-aggregator.urls="https://capx-mainnet.calderachain.xyz/rest-aggregator"
      - --parent-chain.blob-client.beacon-url=[Your Beacon RPC]
      - --parent-chain.connection.url=[Your Ethereum RPC]
